#!/bin/sh
LH=/usr/lib/libhooker.dylib
if test -f "$LH"; then
  launchctl unload /Library/LaunchDaemons/lh_moe.panties.discordextrasserver.plist
else
  launchctl unload /Library/LaunchDaemons/cy_moe.panties.discordextrasserver.plist
fi
